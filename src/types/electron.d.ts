// Legacy stubs — electron.ts is the canonical source of truth.
// This file only re-exports to avoid stale duplicate declarations.
export type {
  RequestConfig,
  ResponseData,
  SavedRequest,
  Collection,
  Environment,
  HistoryEntry,
  Payload,
  ResponseCapture,
  AuthConfig,
  BodyType,
  RawLanguage,
  FlowStep,
  Flow,
  FlowStepExecutionDetail,
  FlowRunStepResult,
  FlowRunState,
  FlowTab,
  SessionState,
  RequestTab,
} from "./electron";
